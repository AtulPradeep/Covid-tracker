describe("generating informations from API", () => {
  it("should generate informations from API", () => {
    //Visit access test case
    cy.visit("localhost:3000");

    // choose city
    // cy.findByRole("button", {
    //   name: /generate/i,
    // });
    //change from date
    //change to dates
    //click generate
    //change chart type
    //download as csv
  });
});
